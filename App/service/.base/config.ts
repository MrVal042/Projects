const baseUrl = '';

console.log('baseUrl:', baseUrl);

export const Endpoints = {
  kyc: `${baseUrl}Kyc/`,
  auth: `${baseUrl}Auth/`,
  user: `${baseUrl}User/`,
  cart: `${baseUrl}Cart/`,
  card: `${baseUrl}Card/`,
  shop: `${baseUrl}Shop/`,
  order: `${baseUrl}Order/`,
};
